This is an app that allows users to log in with their email, and that could be a magic link or a 6-digit code. I think the sample key file shows a magic link approach, so it can use the same way. Then their email domain will become their company name, which creates a company that has different users. The first user log in can be an admin. They can add other users and assign different roles to them, such as viewer or editor. The admin always can access anything they would have in a SaaS software.

On this sidebar, there's a top bar on the top part that will be log-in and out and their profile icon. On the sidebar, there will be the first tab called "Transcribe". They could transcribe, they'll be able to upload one or multiple files. When they upload that, it could create a new project. A project could have a name, a bit of description, and the project could be shared to users or to the whole organisation.

In the project, they would upload one or more files, they can add files in later as well. But when they upload one or more files, then the file can be transcribed. I think that there's a limit, look at the sony aux.md file, there is a limit of 60 minutes, so if the file is greater than 60 minutes, we use FME PG to split the file by let's say 15 minutes interval. But if less than 15 minutes and that's fine, we would transcribe them. We will use Gemini drag API to index those documents, search for that index and combine. Give an option for the user to combine all the text together into one document. Use Gemini drag or some way to have the raw document but also have a structured document. Structured document means it uses AI to make sense of a document, divide them into sections, chapters, and identify speaker from context. So make it create an effective and clean transcript. Store them into some sort of data structure. There will be the rack data structure that allows for semantic retrieval. There'll be a chapter or section page-based structure that allows people to browse like chapters of a book or a transcript or an actual conference with different sections. And there'll be a concept map maps that allows people to understand the concepts like a mind map. 